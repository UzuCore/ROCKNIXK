--- a/grub-core/normal/menu_text.c	2026-05-22 06:44:02.811556829 +0900
+++ b/grub-core/normal/menu_text.c	2026-05-22 06:44:02.843956829 +0900
@@ -181,17 +181,17 @@
       if (nested)
 	{
 	  ret += grub_print_message_indented_real
-	    (_("Press enter to boot the selected OS, "
-	       "`e' to edit the commands before booting "
-	       "or `c' for a command-line. ESC to return previous menu."),
+	    (_("Press the Power button to boot the selected Device, "
+	       "the selected device will be set as the default "
+	       "after the first full boot."),
 	     STANDARD_MARGIN, STANDARD_MARGIN, term, dry_run);
 	}
       else
 	{
 	  ret += grub_print_message_indented_real
-	    (_("Press enter to boot the selected OS, "
-	       "`e' to edit the commands before booting "
-	       "or `c' for a command-line."),
+	    (_("Press the Power button to boot the selected Device, "
+	       "the selected device will be set as the default "
+	       "after the first full boot."),
 	     STANDARD_MARGIN, STANDARD_MARGIN, term, dry_run);
 	}
     }
